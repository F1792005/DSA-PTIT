#include <bits/stdc++.h>
using namespace std;
char c;
int x[101];
int 
int daxet[101]
void Try(int i){
    for(char j = x[i-1]+1;j<=;j++){
        if(daxet[i]==0){
            x[i] = j;
            if(i==c){
                in()
            }
        }
    }
}
int main(){
    cin >> c;
    for(char cs = 'A',cs<=c;cs++){
        
    }
    memset(daxet,0,sizeof(daxet));
    Try(1);
}